import { Box, Button } from "@chakra-ui/react";
import React from "react";

type AppProps = {};

const App: React.FC<AppProps> = () => {
  return (
    <Box m={2}>
      <Button>hehe</Button>
    </Box>
  );
};
export default App;
